// Launch-time feature flags. Flip these back on for the official paid launch.

// Referral / Parrainage program — hidden during the free beta launch (no payments
// yet, so cashback can't be earned). Code is kept intact; just not surfaced.
export const REFERRALS_ENABLED = false;
